//高精度的加减乘除
//关键之处在于next()函数，即记录P串的重复性（仅与P串有关）
//1，生成next（）函数
//2.进入循环，以i < n为结束
//3，判断，若匹配则各自加1，失配则，j回到next()。
//4.其中，next()函数，实现同样如此

#include<iostream>
#include<cstring>
#include<algorithm>
#include<vector>
#include<ctime>
#include<time.h>

using namespace std;


int main(){

    printf("mother fucker C 艹\n");
    system("pause");
}